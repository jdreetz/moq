
import type { Full, SupportMode } from "@moq/hang/support";

type SupportDetailsProps = {
    support: Full;
    mode: SupportMode;
}

export default function SupportDetails(props: SupportDetailsProps) {
    return <h1>Support Details</h1>
}
    